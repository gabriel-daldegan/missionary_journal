---
story_id: "1.1"
epic: "Epic 1: Tenant-Scoped Memory Timeline MVP"
title: "Tenant-Safe Memory Route and Layout Shell"
status: "Ready for Review"
jira:
  project_key: "MIS"
  project_id: "10148"
  cloud_id: "287a75f4-4fb2-497a-986e-82a5e566d271"
  site_url: "https://vulpelabs.atlassian.net"
  issue_key: "MIS-2"
  parent_key: "MIS-1"
  issue_type: "História"
  issue_type_id: "10116"
  category: "memory"
  tags:
    - "story"
    - "project-missionary-journal"
    - "epic-1"
    - "story-1-1"
    - "backend"
    - "frontend"
    - "rules"
    - "tenant-scope"
    - "routing"
    - "layout-shell"
    - "privacy"
  labels:
    - "story"
    - "project-missionary-journal"
    - "epic-1"
    - "story-1-1"
    - "category-memory"
    - "backend"
    - "frontend"
    - "rules"
    - "tenant-scope"
    - "routing"
    - "layout-shell"
    - "privacy"
  jira_fields:
    category: "customfield_10053"
    tags: "customfield_10246"
    labels: "labels"
  url: "https://vulpelabs.atlassian.net/browse/MIS-2"
  last_sync: "2026-05-19T10:22:41-03:00"
  sync_note: "Jira Epic MIS-1 and child História MIS-2 created through Atlassian MCP."
---

# Story 1.1: Tenant-Safe Memory Route and Layout Shell

## Status

Ready for Review

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools:
  - "php artisan test --compact tests/Feature/Memory/MemoryRouteAccessTest.php"
  - "vendor/bin/pint --dirty --format agent"
  - "php artisan route:list --name=memories"
```

## Story

**As a** registered user,
**I want** to enter a dedicated memory product area for my workspace,
**so that** my memories are accessed through the correct tenant boundary and do not feel like an admin dashboard.

## Acceptance Criteria

1. Authenticated users can access a memory route group under `/memories/{tenant:uuid}`.
2. Unauthenticated visitors are redirected to authentication before reaching memory routes.
3. Users who are not members of the route tenant cannot access the memory area.
4. The memory area uses a dedicated authenticated layout, proposed as `resources/views/components/layouts/memory.blade.php`.
5. The layout includes workspace context, product navigation, profile or language access, and an account/dashboard escape path.
6. The layout does not include the public marketing footer, public hero treatment, Filament CRUD chrome, or table-first dashboard structure.

## Integration Verification

1. **IV1:** Existing Filament Dashboard routes for billing, account, users, roles, teams, and invitations still work.
2. **IV2:** Existing public marketing and authentication routes still render through their current layouts.
3. **IV3:** Feature tests cover authenticated access, unauthenticated redirect, and cross-tenant denial.

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled in `core-config.yaml`.
> Quality validation will use manual review process only.
> To enable, set `coderabbit_integration.enabled: true` in core-config.yaml.

## Tasks / Subtasks

- [x] Add the tenant-scoped memory route shell in `routes/web.php`. (AC: 1, 2, 3)
  - [x] Define an authenticated, verified route group under `/memories/{tenant:uuid}` with `memories.` route names.
  - [x] Add the initial route endpoint needed for this story to render the memory shell, using a minimal Livewire full-page component such as `App\Livewire\Memory\MemoryTimeline` as a placeholder only.
  - [x] Keep profile setup, record CRUD, media streaming, and filters out of this story except for route/navigation placeholders needed by the shell.
  - [x] Use named routes and route model binding; do not add a SPA router, JSON API client, or JavaScript state store.

- [x] Enforce tenant membership before any memory shell content renders. (AC: 1, 3)
  - [x] Create a small middleware such as `App\Http\Middleware\EnsureTenantMember` and register it using the project's Laravel 13 middleware convention.
  - [x] Reuse existing SaaSykit tenant membership primitives, especially `User::canAccessTenant(Model $tenant)` and the `Tenant::users()` relationship, instead of introducing a parallel collaboration system.
  - [x] Return one generic denial response for non-members and ensure the response does not reveal tenant-private details.

- [x] Create the dedicated authenticated memory layout. (AC: 4, 5, 6)
  - [x] Add `resources/views/components/layouts/memory.blade.php`.
  - [x] Include workspace context, memory product navigation, profile or language access, and an escape path to the existing dashboard/account area.
  - [x] Include shared head/tail concerns as appropriate from the existing Blade layout conventions, but do not include the public marketing footer, public hero treatment, Filament chrome, or table-first dashboard structure.
  - [x] Use Tailwind CSS 4 utility classes in Blade, prefer `gap-*` spacing between siblings, keep cards at `8px` radius or less, and use visible focus rings for interactive controls.

- [x] Wire the minimal Livewire memory shell to the dedicated layout. (AC: 1, 4, 5, 6)
  - [x] Create the component using the existing Livewire convention for this application; for Livewire 4, check `php artisan make:livewire --help` and prefer the class-based format if it matches the architecture guidance.
  - [x] Render the shell through `components.layouts.memory`.
  - [x] Keep server-side tenant state authoritative; validate and authorize as in normal Laravel requests.
  - [x] Do not create timeline grouping, records, profile persistence, media handling, or localization tables in this story.

- [x] Preserve existing SaaSykit surfaces. (IV: 1, 2)
  - [x] Confirm `/dashboard` still redirects through `UserDashboardService` and the Filament Dashboard remains responsible for billing, account, workspace settings, users, roles, teams, and invitations.
  - [x] Confirm public marketing, authentication, blog, checkout, roadmap, and invitation routes still use their current layouts and route behavior.

- [x] Add focused PHPUnit feature coverage. (AC: 1, 2, 3, 4, 6; IV: 1, 2, 3)
  - [x] Add `tests/Feature/Memory/MemoryRouteAccessTest.php`.
  - [x] Cover guest redirect for the memory route.
  - [x] Cover tenant member access to the memory shell.
  - [x] Cover authenticated non-member denial without tenant-private details.
  - [x] Cover that the member response uses the memory layout and does not render the public marketing footer/hero or Filament/table-first chrome.
  - [x] Add at least one regression assertion that existing public and dashboard entry routes still respond or redirect through their current behavior.

## Dev Notes

### Previous Story Insights

- No previous implementation story exists in `docs/stories/`; this is the first story in Epic 1.
- Code intelligence duplicate detection was checked and is unavailable in this session, so no duplicate advisory was generated.
- Jira project `MIS` is configured through Atlassian MCP; Epic `MIS-1` and child História `MIS-2` were created and synced for this story on 2026-05-19. The Jira Category field remains declared in frontmatter, while the active functional/area mapping is represented through Jira tags and labels, including `category-memory`.

### Requirements Context

- Epic 1 builds a private, tenant-scoped memory timeline surface inside the existing SaaSykit Laravel application while preserving existing account, billing, invitation, dashboard, and tenancy behavior. [Source: docs/prd.md#epic-1-tenant-scoped-memory-timeline-mvp]
- Story 1.1 specifically requires `/memories/{tenant:uuid}` access, guest redirect, non-member denial, a dedicated authenticated layout, workspace/product navigation, profile or language access, dashboard escape, and explicit avoidance of marketing/Filament/table-first chrome. [Source: docs/prd.md#story-11-tenant-safe-memory-route-and-layout-shell]
- Integration verification for this story must preserve existing Filament Dashboard, public marketing, authentication routes, and cover authenticated access, guest redirect, and cross-tenant denial with feature tests. [Source: docs/prd.md#story-11-tenant-safe-memory-route-and-layout-shell]

### Architecture Guidance

- AIOX Dev-facing implementation context is now summarized in `docs/framework/coding-standards.md`, `docs/framework/source-tree.md`, and `docs/framework/tech-stack.md`; these shards are derived from the richer architecture documents and should be used for implementation traceability. [Source: docs/framework/coding-standards.md#coding-standards; docs/framework/source-tree.md#source-tree; docs/framework/tech-stack.md#technical-stack]
- The memory product should use `/memories/{tenant:uuid}` as the customer-facing route group while SaaSykit account operations stay in Filament Dashboard. [Source: docs/architecture.md#product-routing-and-ui-architecture]
- Recommended route shape uses Laravel named routes, route model binding, `auth`, `verified`, tenant membership checks, and the `memories.` name prefix. [Source: docs/framework/source-tree.md#route-shape]
- Middleware names in the architecture are placeholders; the rule is that tenant membership and profile completion are enforced through route-level/server-side checks, not Blade-only decisions. [Source: docs/framework/coding-standards.md#memory-product-rules]
- The route shell story's required test slice is guest redirect, member access, non-member denial, dedicated memory layout, and existing Filament/account route regression. [Source: docs/ui-architecture.md#required-coverage-by-story-type; docs/framework/coding-standards.md#tests]

### Existing Codebase Context

- `routes/web.php` currently defines public, auth, checkout, blog, roadmap, invitation, invoice, and `/dashboard` routes; there is no memory route group yet. [Source: routes/web.php]
- `User::canAccessTenant(Model $tenant): bool` checks tenant membership through the existing `tenants()` relationship. [Source: app/Models/User.php]
- `Tenant::users()` is a `BelongsToMany` relationship using the existing `tenant_user` pivot model. [Source: app/Models/Tenant.php]
- Existing feature tests extend `Tests\Feature\FeatureTest`, which runs migrations/seeders, configures currency, disables exception handling and Vite, and provides `createUser()` and `createTenant()` helpers. [Source: tests/Feature/FeatureTest.php]

### UI and Layout Guidance

- `resources/views/components/layouts/memory.blade.php` is the customer-facing memory shell and must not place memory screens inside the public marketing layout or Filament Dashboard chrome. [Source: docs/framework/coding-standards.md#memory-product-rules]
- The memory layout shell owns navigation, workspace context, profile/language access, and dashboard/account escape; individual screens should not duplicate this chrome. [Source: docs/framework/coding-standards.md#component-boundaries]
- The MVP layout should be authenticated, tenant-aware, compact, product-first, and free of public marketing footer or hero section. [Source: docs/architecture.md#product-routing-and-ui-architecture]
- Use custom Livewire/Blade product UI for memory workflows; Filament remains for billing, account, workspace settings, users, roles, teams, invitations, security, and admin/reporting. [Source: docs/framework/tech-stack.md#memory-product-direction]
- Tailwind implementation should use existing utility conventions, prefer neutral surfaces, avoid public marketing treatment, and preserve readable responsive layouts. [Source: docs/framework/coding-standards.md#tailwind-and-ui]

### Livewire Guidance

- Use Livewire for server-driven memory screens and keep tenant state server-side. [Source: docs/framework/tech-stack.md#runtime]
- Canonical Dev context points memory Livewire classes to `app/Livewire/Memory` and views to `resources/views/livewire/memory`. [Source: docs/framework/source-tree.md#memory-product-locations]
- Livewire 4 supports full-page components and class-based generation, but the developer should verify the local `make:livewire` options before generating files. [Source: .agents/skills/livewire-development/SKILL.md#creating-components]
- Livewire loops in future screens should use `wire:key`, loading states should use Livewire loading primitives, and actions must authorize/validate like HTTP requests; this story only needs a shell, so do not introduce premature client-side behavior. [Source: .agents/skills/livewire-development/SKILL.md#best-practices]

### Project Structure Notes

- Primary memory route file: `routes/web.php`. [Source: docs/framework/source-tree.md#memory-product-locations]
- Dedicated memory layout path: `resources/views/components/layouts/memory.blade.php`. [Source: docs/framework/source-tree.md#memory-product-locations]
- Memory Livewire classes: `app/Livewire/Memory`. [Source: docs/framework/source-tree.md#memory-product-locations]
- Memory Livewire views: `resources/views/livewire/memory`. [Source: docs/framework/source-tree.md#memory-product-locations]
- Memory route tests: `tests/Feature/Memory`. [Source: docs/framework/source-tree.md#memory-product-locations]
- No structural conflict found: these paths fit the existing Laravel routes, Blade layout, Livewire, and PHPUnit organization.
- No new environment variables are required for this story.

### Scope Boundaries

- Do not implement profile setup persistence or profile gating beyond placeholders needed by the layout; Story 1.2 owns profile setup.
- Do not implement workspace shared-editing settings; Story 1.3 owns the policy foundation.
- Do not create diary/trip/media/filter/localization/reporting domain behavior; later Epic 1 stories own those features.
- Do not add per-record missionary-vs-family visibility controls.
- Do not use visible album language in MVP UI copy.
- Do not introduce a new frontend starter, SPA, API client, JavaScript store, or public sharing surface.

### Testing

- Use PHPUnit classes only. [Source: docs/framework/coding-standards.md#tests]
- Use factories and project helpers for users and tenants; `FeatureTest::createTenant()` and `FeatureTest::createUser($tenant)` are available. [Source: tests/Feature/FeatureTest.php]
- Test happy paths and authorization failures separately. [Source: docs/framework/coding-standards.md#tests]
- Denied states must assert both the denial behavior and absence of private tenant or record details. [Source: docs/ui-architecture.md#testing-requirements; docs/framework/coding-standards.md#memory-product-rules]
- Required focused command for this story: `php artisan test --compact tests/Feature/Memory/MemoryRouteAccessTest.php`.
- Formatting command after PHP edits: `vendor/bin/pint --dirty --format agent`.

## Change Log

| Date | Version | Description | Author |
| --- | --- | --- | --- |
| 2026-05-23 | 0.4 | Reconciled source traceability with canonical docs/framework Dev context after AIOX-DOCS-001C. | River |
| 2026-05-22 | 0.3 | Implemented tenant-safe memory route, layout shell, middleware, Livewire shell, and focused route tests. | Dex |
| 2026-05-20 | 0.2 | Corrected source traceability anchors after PO review PO-CORR-002. | River |
| 2026-05-18 | 0.1 | Initial draft from PRD, architecture, UX/frontend architecture, and Architect-to-SM handoff. | River |

## Dev Agent Record

### Agent Model Used

GPT-5 Codex

### Debug Log References

- `php artisan make:livewire --help` confirmed Livewire 4 class-based generation options.
- `docker compose exec -u sail -e DB_CONNECTION=mysql -e DB_HOST=mysql -e DB_DATABASE=testing -e DB_USERNAME=sail_missionaryjournal -e DB_PASSWORD=password_missionaryjournal laravel.test php artisan test --compact tests/Feature/Memory/MemoryRouteAccessTest.php` passed: 5 tests, 23 assertions.
- `vendor/bin/pint --dirty --format agent` passed.
- `php artisan route:list --name=memories` confirmed `GET|HEAD memories/{tenant:uuid}` as `memories.timeline`.
- `docker compose exec -u sail laravel.test ./vendor/bin/phpstan analyse app/Http/Middleware/EnsureTenantMember.php app/Livewire/Memory/MemoryTimeline.php tests/Feature/Memory/MemoryRouteAccessTest.php --memory-limit=1G` passed with no errors.
- `npm run lint` passed.
- `npm run typecheck` failed on existing global PHPStan issues outside this story scope: 387 errors across Filament resources/services.
- `npm test` failed on existing test environment configuration: MySQL rejected `root` without password for `saasykit_tenancy_test`; the repeated failure was interrupted after the cause was confirmed.
- `git diff --check` passed.

### Completion Notes List

- Added the tenant-scoped memory route group under `/memories/{tenant:uuid}` with authenticated, verified, tenant-member guarded `memories.timeline`.
- Added `EnsureTenantMember` middleware using existing SaaSykit tenant access via `User::canAccessTenant()` and a generic 404 denial for non-members.
- Added the minimal class-based Livewire memory shell and dedicated authenticated memory layout, with workspace context, memory navigation, profile access, and dashboard escape.
- Preserved public marketing and Filament dashboard surfaces; no record CRUD, profile persistence, media, filters, localization tables, SPA router, JSON API client, or JavaScript store were added.

### File List

- `app/Http/Middleware/EnsureTenantMember.php`
- `app/Livewire/Memory/MemoryTimeline.php`
- `bootstrap/app.php`
- `resources/views/components/layouts/memory.blade.php`
- `resources/views/livewire/memory/memory-timeline.blade.php`
- `routes/web.php`
- `tests/Feature/Memory/MemoryRouteAccessTest.php`
- `docs/stories/1.1.tenant-safe-memory-route-and-layout-shell.story.md`

## QA Results

_To be populated by the QA Agent._

### Review Date: 2026-05-23

### Reviewed By: Quinn (Test Architect)

### Code Quality Assessment

Story 1.1 is implemented within the approved shell scope. The route group uses `/memories/{tenant:uuid}` with `auth`, `verified`, and `tenant.member`; tenant membership is enforced before the Livewire shell renders; and the memory UI uses a dedicated Blade layout rather than public marketing or Filament dashboard chrome. No record CRUD, profile persistence, media handling, filters, localization tables, SPA router, JSON API client, or JavaScript store were introduced.

Gate is `WAIVED` because story-focused behavior is covered and the repository-wide `npm run typecheck` timeout plus `npm test` failures were explicitly approved as external to Story 1.1.

### Refactoring Performed

- **File**: `bootstrap/app.php`
  - **Change**: Removed an accidental `## adicionado` comment beside the new middleware import.
  - **Why**: The comment was non-functional noise in application bootstrap code.
  - **How**: Keeps the middleware registration focused and aligned with the surrounding Laravel bootstrap style.

### Compliance Check

- Coding Standards: ✓ Uses explicit return types, existing route/middleware conventions, named routes, and server-side tenant authorization.
- Project Structure: ✓ Uses the approved memory locations for route, middleware, Livewire class, Blade layout, Livewire view, and feature tests.
- Testing Strategy: ✓ Focused PHPUnit feature coverage maps to guest redirect, member access, cross-tenant denial, dedicated layout boundaries, and existing route regression.
- All ACs Met: ✓ Acceptance Criteria 1-6 are covered by implementation and tests.

### Validation Evidence

- `npm run lint` passed via Sail Pint.
- `docker compose exec -u sail -e DB_CONNECTION=mysql -e DB_HOST=mysql -e DB_DATABASE=testing -e DB_USERNAME=sail_missionaryjournal -e DB_PASSWORD=password_missionaryjournal laravel.test php artisan test --compact tests/Feature/Memory/MemoryRouteAccessTest.php` passed: 5 tests, 23 assertions.
- `docker compose exec -u sail laravel.test ./vendor/bin/phpstan analyse app/Http/Middleware/EnsureTenantMember.php app/Livewire/Memory/MemoryTimeline.php tests/Feature/Memory/MemoryRouteAccessTest.php --memory-limit=1G` passed.
- `php artisan route:list --name=memories` passed and showed `memories.timeline`.
- `git diff --check` passed.
- `npm run typecheck` failed: PHPStan parallel worker timed out after 600 seconds.
- `npm test` was stopped after it emitted repeated failures for more than three minutes without a useful summary.

### Improvements Checklist

- [x] Verified tenant-scoped route registration and `memories.timeline` naming.
- [x] Verified non-member denial uses a generic not-found response without tenant-private content.
- [x] Verified dedicated memory layout avoids public footer/hero, Filament chrome, and table-first structure.
- [x] Removed accidental bootstrap comment during QA review.
- [ ] Resolve or explicitly waive the repository-wide `npm run typecheck` timeout and `npm test` failures before any merge/release policy that requires full green checks.

### Security Review

No blocking security issue found in the Story 1.1 implementation. The route is authenticated and verified, tenant lookup uses UUID route binding, and `EnsureTenantMember` reuses `User::canAccessTenant()` before private shell content renders. The non-member test asserts both `404` and absence of tenant-private details.

### Performance Considerations

No performance concern for this shell. The implementation adds only the memory route, a single membership existence check, and static shell markup; it does not introduce timeline queries, media access, polling, or client-side state.

### Files Modified During Review

- `bootstrap/app.php`
- `docs/qa/gates/1.1-tenant-safe-memory-route-and-layout-shell.yml`
- `docs/stories/1.1.tenant-safe-memory-route-and-layout-shell.story.md`
- `.aiox/handoffs/20260523T081711-0300-dev-to-qa-story-1-1-ready-for-review.yaml`
- `.aiox/handoffs/20260523T085412-0300-qa-to-devops-story-1-1-waived-push.yaml`

### Gate Status

Gate: WAIVED → docs/qa/gates/1.1-tenant-safe-memory-route-and-layout-shell.yml

### Recommended Status

✓ Ready for Done. External repository gate issues were explicitly waived by approver.
