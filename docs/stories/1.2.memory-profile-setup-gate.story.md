---
story_id: "1.2"
epic: "Epic 1: Tenant-Scoped Memory Timeline MVP"
title: "Memory Profile Setup Gate"
status: "Ready for Review"
jira:
  project_key: "MIS"
  project_id: "10148"
  cloud_id: "287a75f4-4fb2-497a-986e-82a5e566d271"
  site_url: "https://vulpelabs.atlassian.net"
  issue_key: "MIS-3"
  parent_key: "MIS-1"
  previous_story_key: "MIS-2"
  issue_type: "História"
  issue_type_id: "10116"
  category: "memory"
  tags:
    - "story"
    - "project-missionary-journal"
    - "epic-1"
    - "story-1-2"
    - "backend"
    - "frontend"
    - "rules"
    - "tenant-scope"
    - "profile-setup"
    - "locale"
    - "gating"
    - "privacy"
  labels:
    - "story"
    - "project-missionary-journal"
    - "epic-1"
    - "story-1-2"
    - "category-memory"
    - "backend"
    - "frontend"
    - "rules"
    - "tenant-scope"
    - "profile-setup"
    - "locale"
    - "gating"
    - "privacy"
  jira_fields:
    category: "customfield_10053"
    tags: "customfield_10246"
    labels: "labels"
  url: "https://vulpelabs.atlassian.net/browse/MIS-3"
  last_sync: "2026-06-09T02:45:53-03:00"
  sync_note: "Jira child story MIS-3 created and synced through Atlassian MCP."
---

# Story 1.2: Memory Profile Setup Gate

## Status

Ready for Review

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools:
  - "php artisan test --compact tests/Feature/Memory/MemoryProfileSetupGateTest.php"
  - "php artisan test --compact tests/Feature/Livewire/Memory/MemoryProfileSetupTest.php"
  - "vendor/bin/pint --dirty --format agent"
  - "php artisan route:list --name=memories"
```

## Story

**As a** new user,
**I want** to complete a lightweight memory profile before using the timeline,
**so that** the product can personalize the memory workspace without forcing mission setup as the global product object.

## Acceptance Criteria

1. A new user without a completed memory profile is routed to profile setup before timeline access.
2. Profile setup captures only MVP-critical information, including display name and preferred interface language.
3. Mission context is optional and does not block completion.
4. Completing setup redirects the user to the tenant timeline.
5. Existing authentication and account-profile behavior remains available.

## Integration Verification

1. **IV1:** Existing user registration and login flows still complete successfully.
2. **IV2:** Profile gating applies only to the memory product surface and does not block Filament account/billing operations.
3. **IV3:** Tests cover setup gating, successful completion, and redirect to the correct tenant timeline.

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled in `core-config.yaml`.
> Quality validation will use manual review process only.
> To enable, set `coderabbit_integration.enabled: true` in core-config.yaml.

## Tasks / Subtasks

- [x] Add memory profile persistence for product onboarding. (AC: 2, 3)
  - [x] Create a `memory_profiles` migration using the architecture fields: `id`, `user_id`, `display_name`, `preferred_locale`, `mission_context`, `completed_at`, and timestamps.
  - [x] Keep the profile user-scoped unless architecture is updated; the documented profile fields do not include `tenant_id`.
  - [x] Make `mission_context` nullable and ensure it is never required for completion.
  - [x] Default `preferred_locale` to `en` and validate support for `en`, `pt`, and `es`.
  - [x] Add the Eloquent model, factory, and useful `User` relationship with explicit return types.

- [x] Add a stateless memory profile service. (AC: 1, 2, 3, 4)
  - [x] Create `App\Services\MemoryProfileService`.
  - [x] Add service methods for creating/updating the profile, determining profile completion, and resolving the preferred locale.
  - [x] Use Eloquent models and relationships before query builder access.
  - [x] Keep validation rules in Livewire or a Form Request boundary; keep persistence and completion decisions in the service.

- [x] Align the memory route group with the profile gate route shape. (AC: 1, 4; IV: 2, 3)
  - [x] Add `memories.profile.setup` at `/memories/{tenant:uuid}/profile/setup` for tenant members before profile completion.
  - [x] Gate the timeline route behind profile completion and expose it as `memories.timeline`, preferably at `/memories/{tenant:uuid}/timeline`.
  - [x] Keep the default `/memories/{tenant:uuid}` entry point useful by redirecting members without a completed profile to setup and completed members to timeline.
  - [x] Preserve `auth`, `verified`, tenant membership checks, route model binding, and the `memories.` route name prefix.
  - [x] Do not gate Filament Dashboard, account, billing, teams, users, roles, invitations, security, or subscription routes.

- [x] Enforce profile completion server-side. (AC: 1, 4; IV: 2, 3)
  - [x] Create `App\Http\Middleware\EnsureMemoryProfileComplete`.
  - [x] Register a `memory.profile.complete` middleware alias using the Laravel 13 middleware convention in `bootstrap/app.php`.
  - [x] Redirect incomplete tenant members to `memories.profile.setup` for the same route tenant.
  - [x] Let completed users continue to timeline and later memory product routes.
  - [x] Avoid Blade-only gating and avoid redirects that leak tenant-private details.

- [x] Build the Livewire memory profile setup screen. (AC: 2, 3, 4, 5; IV: 1, 2)
  - [x] Create a class-based `App\Livewire\Memory\MemoryProfileSetup` component and `resources/views/livewire/memory/memory-profile-setup.blade.php`.
  - [x] Render through `components.layouts.memory` and keep the UI product-first, not Filament CRUD or public marketing treatment.
  - [x] Capture display name and preferred interface language as required fields.
  - [x] Capture optional mission context without blocking save when it is blank.
  - [x] Validate in the Livewire action, delegate persistence to `MemoryProfileService`, and redirect to `memories.timeline` for the same tenant after completion.
  - [x] Keep the existing account profile route available; do not replace SaaSykit account-profile behavior with the memory profile form.

- [x] Add minimal memory UI localization support for setup. (AC: 2, 5)
  - [x] Add `lang/en/memory.php`, `lang/pt/memory.php`, and `lang/es/memory.php` entries needed by the profile setup screen and memory layout controls touched by this story.
  - [x] Use translation keys in the new Blade view and avoid hard-coded customer-facing memory UI copy.
  - [x] Store the selected interface language in `memory_profiles.preferred_locale`; do not translate user-generated mission context.
  - [x] Add locale application for authenticated memory product routes only if it can stay within this story's profile setup boundary.

- [x] Update the existing memory shell without expanding timeline scope. (AC: 1, 4, 5; IV: 2)
  - [x] Update `MemoryTimeline` only as needed for the new route and profile gate.
  - [x] Keep the dedicated memory layout's workspace context, product navigation, profile/language access, and dashboard escape.
  - [x] Do not implement timeline grouping, diary records, trip/period records, media, filters, public sharing, or a JavaScript domain store in this story.

- [x] Add focused PHPUnit and Livewire coverage. (AC: 1, 2, 3, 4, 5; IV: 1, 2, 3)
  - [x] Add `tests/Feature/Memory/MemoryProfileSetupGateTest.php`.
  - [x] Cover member without completed profile redirected from timeline/default memory entry to setup.
  - [x] Cover member can access setup before profile completion.
  - [x] Cover successful profile completion persists display name, preferred locale, optional mission context, and `completed_at`, then redirects to the correct tenant timeline.
  - [x] Cover mission context is optional.
  - [x] Cover completed profile can access timeline.
  - [x] Cover non-member still receives generic denial and does not see tenant-private details.
  - [x] Cover profile gating does not block the existing dashboard/account/billing entry behavior.
  - [x] Add `tests/Feature/Livewire/Memory/MemoryProfileSetupTest.php` for Livewire validation, required fields, supported locales, and redirect behavior if the route feature test does not cover component state directly.

## Dev Notes

### Previous Story Insights

- Story 1.1 created the tenant-scoped memory route shell, `EnsureTenantMember` middleware, `MemoryTimeline` Livewire component, dedicated memory layout, and focused route tests. [Source: docs/stories/1.1.tenant-safe-memory-route-and-layout-shell.story.md#completion-notes-list]
- Story 1.1 is still marked `Ready for Review` in the story file, but the DevOps handoff for Story 1.2 records PR #4 as merged with a successful release check and QA gate waived for repo-wide debt external to Story 1.1. This draft proceeds because the user explicitly requested Story 1.2. [Source: .aiox/handoffs/20260523T094742-0300-devops-to-sm-story-1-2-ready-to-draft.yaml#known-quality-context]
- QA recommended adding dedicated Livewire component tests when Story 1.2 introduces interactive profile setup or stateful memory actions. [Source: docs/qa/gates/1.1-tenant-safe-memory-route-and-layout-shell.yml#recommendations]
- Code intelligence duplicate detection was checked and is unavailable in this session, so no duplicate advisory or suggested-file enrichment was generated.

### Requirements Context

- Epic 1 builds a private, tenant-scoped memory timeline surface inside the existing SaaSykit Laravel application while preserving account, billing, invitation, dashboard, and tenancy behavior. [Source: docs/prd.md#epic-1-tenant-scoped-memory-timeline-mvp]
- Story 1.2 requires routing new users without a completed memory profile to setup before timeline access, collecting display name and preferred interface language, keeping mission context optional, redirecting to the tenant timeline after setup, and preserving existing auth/account behavior. [Source: docs/prd.md#story-12-memory-profile-setup-gate]
- Integration verification must preserve registration/login, limit profile gating to the memory product surface, and test setup gating, successful completion, and redirect to the correct tenant timeline. [Source: docs/prd.md#story-12-memory-profile-setup-gate]

### Architecture Guidance

- `memory_profiles` stores MVP profile completion for product onboarding with `user_id`, `display_name`, `preferred_locale`, optional `mission_context`, and `completed_at`. [Source: docs/architecture.md#memory_profiles]
- `preferred_locale` defaults to `en` and supports `en`, `pt`, and `es`; users should be able to select it during registration/profile setup and change it later. [Source: docs/architecture.md#memory_profiles]
- `MemoryProfileService` should create/update profiles, determine setup completion, and resolve locale preference. [Source: docs/architecture.md#service-architecture]
- Setup route must stay reachable before profile completion, while timeline/editor/detail routes are gated after setup. [Source: docs/ui-architecture.md#route-configuration]
- Route responsibilities define `memories.profile.setup` for display name, preferred UI language, and optional context; `memories.timeline` requires a completed memory profile. [Source: docs/ui-architecture.md#route-responsibilities]
- The default memory entry should redirect users without completed profiles to setup and completed users to `memories.timeline`. [Source: docs/ui-architecture.md#navigation-rules]
- Redirect behavior for incomplete profile access is setup redirect for the same tenant; non-member tenant access remains generic denial/not-found. [Source: docs/ui-architecture.md#redirect-and-denial-behavior]
- Use Laravel localization for UI and navigation only; user-generated content stays in the language entered by the user. [Source: docs/architecture.md#localization-architecture; docs/framework/tech-stack.md#memory-product-direction]

### Existing Codebase Context

- `routes/web.php` currently defines `/memories/{tenant:uuid}` with `auth`, `verified`, and `tenant.member`, and maps the root route to `MemoryTimeline` as `memories.timeline`. Story 1.2 may refactor this into a default entry route plus `/profile/setup` and `/timeline`.
- `bootstrap/app.php` already registers the `tenant.member` alias for `EnsureTenantMember`; Story 1.2 should register `memory.profile.complete` the same way.
- `App\Livewire\Memory\MemoryTimeline` already accepts the route `Tenant` in `mount()` and renders through `components.layouts.memory`.
- `resources/views/components/layouts/memory.blade.php` and `resources/views/livewire/memory/memory-timeline.blade.php` exist from Story 1.1 and should be adjusted only as needed for profile/language access.
- `App\Models\User` currently has `tenants()` and `canAccessTenant()` but no memory profile relationship.
- The repository currently has only `lang/en/auth.php`, `lang/en/pagination.php`, `lang/en/passwords.php`, and `lang/en/validation.php`; memory UI translation files do not exist yet.

### Project Structure Notes

- Memory routes: `routes/web.php`. [Source: docs/framework/source-tree.md#memory-product-locations]
- Profile gate middleware: `app/Http/Middleware/EnsureMemoryProfileComplete.php`. [Source: docs/framework/source-tree.md#memory-product-locations]
- Memory Livewire classes: `app/Livewire/Memory`. [Source: docs/framework/source-tree.md#memory-product-locations]
- Memory Livewire views: `resources/views/livewire/memory`. [Source: docs/framework/source-tree.md#memory-product-locations]
- Memory services: `app/Services` with `Memory*Service` names. [Source: docs/framework/source-tree.md#memory-product-locations]
- Memory route tests: `tests/Feature/Memory`; memory Livewire tests: `tests/Feature/Livewire/Memory`. [Source: docs/framework/source-tree.md#memory-product-locations]
- No new environment variables are required for this story.

### Scope Boundaries

- Draft and implement only the memory profile setup gate; do not implement diary records, trip/period records, timeline grouping, media, filters, reporting, public sharing, or workspace collaboration settings.
- Do not add per-record missionary-vs-family visibility controls.
- Do not make mission context required and do not treat mission setup as the global product object.
- Do not use visible album-generation language in MVP UI copy.
- Do not add a SPA router, JSON API client, new JavaScript state store, or second frontend runtime.
- Do not move billing, account, workspace settings, users, roles, teams, invitations, security, subscriptions, or admin/reporting out of Filament.

### Testing

- Use PHPUnit classes only. [Source: docs/framework/coding-standards.md#tests]
- Profile setup stories require setup gate, successful profile completion, locale persistence, redirect to timeline, and account/dashboard unaffected coverage. [Source: docs/ui-architecture.md#required-coverage-by-story-type]
- Route tests should include guest redirect, tenant member access, non-member denial, profile gate redirect, and completed profile redirect where relevant. [Source: docs/ui-architecture.md#route-tests-required-by-stories]
- Use Livewire tests for component state, validation, actions, redirects, and rendered UI state. [Source: docs/ui-architecture.md#testing-best-practices]
- Use factories and project helpers for users and tenants; `FeatureTest::createTenant()` and `FeatureTest::createUser($tenant)` are already used by Story 1.1 route tests. [Source: tests/Feature/Memory/MemoryRouteAccessTest.php]
- Required focused commands for this story:
  - `php artisan test --compact tests/Feature/Memory/MemoryProfileSetupGateTest.php`
  - `php artisan test --compact tests/Feature/Livewire/Memory/MemoryProfileSetupTest.php`
  - `php artisan route:list --name=memories`
- Formatting command after PHP edits: `vendor/bin/pint --dirty --format agent`.
- Repo-wide `npm run typecheck` and `npm test` had known external debt during Story 1.1 QA; rerun or formally account for those gates before release/PR policy requires full green checks. [Source: docs/qa/gates/1.1-tenant-safe-memory-route-and-layout-shell.yml#top_issues]

## Change Log

| Date | Version | Description | Author |
| --- | --- | --- | --- |
| 2026-05-23 | 0.2 | Approved by PO after validate-story-draft readiness review. | Pax |
| 2026-05-23 | 0.1 | Initial draft from PRD, architecture, UI architecture, Story 1.1 results, and DevOps-to-SM handoff. | River |
| 2026-05-23 | 0.3 | Implemented memory profile persistence, profile gate routes, Livewire setup, localization, and focused coverage. | Dex |

## Dev Agent Record

### Agent Model Used

GPT-5 Codex

### Debug Log References

- `php artisan route:list --name=memories` - passed; confirmed `memories.entry`, `memories.profile.setup`, and `memories.timeline`.
- `php artisan test --compact tests/Feature/Memory/MemoryProfileSetupGateTest.php` - host run failed before assertions because local MySQL was unavailable (`SQLSTATE[HY000] [2002]`); reran in Sail with test DB overrides.
- `php artisan test --compact tests/Feature/Livewire/Memory/MemoryProfileSetupTest.php` - host run failed before assertions because local MySQL was unavailable (`SQLSTATE[HY000] [2002]`); reran in Sail with test DB overrides.
- `docker compose exec -T -e APP_ENV=testing -e DB_HOST=mysql -e DB_DATABASE=testing -e DB_USERNAME=sail_missionaryjournal -e DB_PASSWORD=password_missionaryjournal laravel.test php artisan test --compact tests/Feature/Memory/MemoryProfileSetupGateTest.php tests/Feature/Livewire/Memory/MemoryProfileSetupTest.php tests/Feature/Memory/MemoryRouteAccessTest.php` - passed; 16 tests, 63 assertions.
- `vendor/bin/pint --dirty --format agent` - passed after applying formatting fixes.
- `docker compose exec -T laravel.test vendor/bin/phpstan analyse app/Http/Middleware/EnsureMemoryProfileComplete.php app/Livewire/Memory/MemoryProfileSetup.php app/Models/MemoryProfile.php app/Services/MemoryProfileService.php --memory-limit=1G` - passed.
- `npm run lint` - passed.
- `npm run typecheck` - failed with PHPStan internal parallel worker timeout after processing all files; no story-specific error was reported, and focused PHPStan passed.
- `npm test` - failed before application assertions because the script uses MySQL `root` without a password against `saasykit_tenancy_test`; this matches the pre-existing repo-wide test DB configuration debt noted in Story 1.1.
- `git diff --check` - passed.

### Completion Notes List

- Implemented user-scoped `memory_profiles` persistence with nullable mission context, supported locale validation, completion timestamp, Eloquent model/factory, and `User::memoryProfile()`.
- Added `MemoryProfileService` plus `memory.profile.complete` middleware so memory timeline routes require completed profiles while setup and non-memory SaaSykit surfaces remain accessible.
- Refactored memory routes to keep `/memories/{tenant:uuid}` as a redirecting entry point, expose `/profile/setup`, and move the gated timeline to `/timeline`.
- Added class-based Livewire profile setup screen through the dedicated memory layout, preserving the existing Filament account profile link.
- Added memory UI translation files under `resources/lang/*/memory.php` because this Laravel app resolves `lang_path()` to `resources/lang`; no user-generated mission context is translated.
- Story DoD self-check completed in YOLO mode: all story-specific requirements, structure, security, focused testing, and story administration items are satisfied; repo-wide `npm run typecheck` and `npm test` remain blocked by external timeout/test database configuration debt, while `npm run lint`, focused PHPStan, and focused Sail tests pass.

### File List

- app/Http/Middleware/EnsureMemoryProfileComplete.php
- app/Livewire/Memory/MemoryProfileSetup.php
- app/Livewire/Memory/MemoryTimeline.php
- app/Models/MemoryProfile.php
- app/Models/User.php
- app/Services/MemoryProfileService.php
- bootstrap/app.php
- database/factories/MemoryProfileFactory.php
- database/migrations/2026_05_23_110927_create_memory_profiles_table.php
- resources/lang/en/memory.php
- resources/lang/es/memory.php
- resources/lang/pt/memory.php
- resources/views/components/layouts/memory.blade.php
- resources/views/livewire/memory/memory-profile-setup.blade.php
- resources/views/livewire/memory/memory-timeline.blade.php
- routes/web.php
- tests/Feature/Livewire/Memory/MemoryProfileSetupTest.php
- tests/Feature/Memory/MemoryProfileSetupGateTest.php
- tests/Feature/Memory/MemoryRouteAccessTest.php

## QA Results

### Review Date: 2026-05-23

### Reviewed By: Quinn (Test Architect)

### Code Quality Assessment

Story 1.2 is implemented within the approved MVP boundary. The memory profile gate is enforced server-side on the memory timeline route, the setup route remains reachable before completion, persistence is user-scoped as required, mission context is optional, and customer-facing memory UI strings use memory translation keys. No out-of-scope diary, media, sharing, filter, SPA, or JavaScript domain-store work was introduced.

The primary residual risk is not in the story implementation: repository-wide scripts remain unreliable under the default package-script environment. Focused story tests, focused PHPStan, auth smoke coverage, route inspection, migration status, lint, and diff checks passed with the Sail test DB credentials.

### Refactoring Performed

None. QA did not modify product code.

### Compliance Check

- Coding Standards: ✓ PHP code uses typed methods, Eloquent relationships, named routes, service-owned profile completion, server-side Livewire validation, and Pint passed.
- Project Structure: ✓ Files are placed in the documented memory middleware, Livewire, service, model, migration, translation, route, and test locations.
- Testing Strategy: ✓ Focused route, Livewire, and existing auth controller tests were executed with passing results under Sail.
- All ACs Met: ✓ AC1-AC5 and IV1-IV3 are covered by implementation review plus focused tests.

### Requirements Traceability

- AC1: Covered by redirect tests from memory entry and timeline to profile setup for users without completed profiles.
- AC2: Covered by Livewire validation and persistence tests for display name and preferred locale.
- AC3: Covered by Livewire completion test with blank mission context persisted as `null`.
- AC4: Covered by Livewire redirect and route tests for completed profile access to the same tenant timeline.
- AC5: Covered by dashboard/account link review and auth/dashboard regression tests.
- IV1: Existing login/register controller tests passed under Sail with the correct test DB credentials.
- IV2: Memory gate middleware is scoped to `memories.timeline`; setup and Filament dashboard/account routes are not gated by profile completion.
- IV3: Focused setup gate and Livewire tests passed.

### Improvements Checklist

- [x] Verified Story 1.2 focused memory route and Livewire coverage in Sail.
- [x] Verified existing login/register controller coverage in Sail for auth regression risk.
- [x] Verified focused PHPStan for story PHP files.
- [x] Verified `npm run lint` / Pint and whitespace checks.
- [ ] Repair repository-wide `npm run typecheck` cache write failure or formally waive it before merge/release policy requires full green checks.
- [ ] Repair `npm test` default test DB credentials or formally waive the repo-wide suite failure before merge/release policy requires full green checks.

### Security Review

PASS. Memory routes remain behind `auth`, `verified`, route-model binding by tenant UUID, and tenant membership middleware. Non-members receive generic not-found responses in route tests, and the Livewire save action re-checks authenticated tenant membership before persistence.

### Performance Considerations

PASS. The story adds one profile lookup/completion check for memory timeline access and does not introduce timeline queries, media processing, client stores, or broad dashboard gating.

### Files Modified During Review

- docs/stories/1.2.memory-profile-setup-gate.story.md
- docs/qa/gates/1.2-memory-profile-setup-gate.yml

### Verification Evidence

- `git diff --check` - passed.
- `git diff --cached --check` - passed.
- `docker compose exec -T laravel.test php artisan route:list --name=memories` - passed; confirmed `memories.entry`, `memories.profile.setup`, and `memories.timeline`.
- `docker compose exec -T -e APP_ENV=testing -e DB_HOST=mysql -e DB_DATABASE=testing -e DB_USERNAME=sail_missionaryjournal -e DB_PASSWORD=password_missionaryjournal laravel.test php artisan migrate:status --path=database/migrations/2026_05_23_110927_create_memory_profiles_table.php` - passed; migration is `Ran`.
- `docker compose exec -T -e APP_ENV=testing -e DB_HOST=mysql -e DB_DATABASE=testing -e DB_USERNAME=sail_missionaryjournal -e DB_PASSWORD=password_missionaryjournal laravel.test php artisan test --compact tests/Feature/Memory/MemoryProfileSetupGateTest.php tests/Feature/Livewire/Memory/MemoryProfileSetupTest.php tests/Feature/Memory/MemoryRouteAccessTest.php` - passed; 16 tests, 63 assertions.
- `docker compose exec -T -e APP_ENV=testing -e DB_HOST=mysql -e DB_DATABASE=testing -e DB_USERNAME=sail_missionaryjournal -e DB_PASSWORD=password_missionaryjournal laravel.test php artisan test --compact tests/Feature/Http/Controllers/Auth/LoginControllerTest.php tests/Feature/Http/Controllers/Auth/RegisterControllerTest.php` - passed; 7 tests, 9 assertions.
- `docker compose exec -T laravel.test vendor/bin/phpstan analyse app/Http/Middleware/EnsureMemoryProfileComplete.php app/Livewire/Memory/MemoryProfileSetup.php app/Models/MemoryProfile.php app/Services/MemoryProfileService.php --memory-limit=1G` - passed.
- `npm run lint` - passed; Pint reported `{"tool":"pint","result":"passed"}`.
- `npm run typecheck` - failed outside Story 1.2 scope with PHPStan internal cache write error under `/tmp/phpstan/cache` while analysing `app/Services/PaymentProviders/Stripe/StripeProvider.php`.
- `npm test` - default repo script remained unusable under its reduced test DB env; a one-test reproduction failed before assertions with MySQL `root` access denied against `saasykit_tenancy_test`.

### Gate Status

Gate: CONCERNS → docs/qa/gates/1.2-memory-profile-setup-gate.yml

### Recommended Status

✓ Ready for Done, with repo-wide quality gate concerns tracked or waived by the story owner before PR/release enforcement.
